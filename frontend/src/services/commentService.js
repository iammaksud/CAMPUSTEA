/**
 * Comment service (architecture placeholder).
 *
 * Planned methods:
 * - getComments(postId)
 * - createComment(postId, content)
 * - deleteComment(id)
 */

export const commentService = {
  // Methods will be implemented in a future phase.
}

export default commentService
