/**
 * Authentication service (architecture placeholder).
 *
 * Planned methods:
 * - register(credentials)
 * - login(credentials)
 * - logout()
 * - getCurrentUser()
 */

export const authService = {
  // Methods will be implemented in a future phase.
}

export default authService
