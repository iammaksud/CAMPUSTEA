/**
 * Category service (architecture placeholder).
 *
 * Planned methods:
 * - getCategories()
 * - getCategory(id)
 * - getCategoryPosts(slug)
 */

export const categoryService = {
  // Methods will be implemented in a future phase.
}

export default categoryService
