/**
 * User service (architecture placeholder).
 *
 * Planned methods:
 * - getMyProfile()
 * - updateProfile(data)
 */

export const userService = {
  // Methods will be implemented in a future phase.
}

export default userService
