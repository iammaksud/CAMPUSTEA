/**
 * MainLayout — public/marketing pages (Landing, Guidelines).
 * Not wired into App.jsx yet; existing layout logic is preserved.
 */
export default function MainLayout({ children }) {
  return <>{children}</>
}
