/**
 * SocialLayout — main app shell with Navbar, Footer, BottomNav.
 * Not wired into App.jsx yet; existing layout logic is preserved in App.jsx.
 */
export default function SocialLayout({ children }) {
  return <>{children}</>
}
