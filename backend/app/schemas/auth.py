"""Authentication schemas (architecture placeholder).

Planned schemas:
- RegisterRequest (email, student_id, password)
- LoginRequest (email, password)
- TokenResponse (access_token, token_type)
- AuthUserResponse (private user fields for /auth/me)
"""

# Schemas will be implemented in a future phase.
