"""User schemas (architecture placeholder).

Planned schemas:
- UserPrivateResponse (full profile for owner — never expose password)
- UserPublicResponse (anonymous_handle only — for posts/comments)
- UserUpdateRequest (editable private fields)
"""

# Schemas will be implemented in a future phase.
