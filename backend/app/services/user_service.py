"""User business logic (architecture placeholder).

Planned responsibilities:
- Get/update private profile (My Space)
- Admin user management (status, role)
- Never expose private fields through public serializers
"""

# Implementation will be added in a future phase.
