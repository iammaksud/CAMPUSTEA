"""FastAPI dependency injection (architecture placeholder).

Planned dependencies:
- get_db: yield a SQLAlchemy session per request
- get_current_user: decode JWT and load authenticated user
- get_current_active_user: ensure user account is active
- require_admin: restrict routes to admin role
"""

# Implementation will be added in a future phase.
