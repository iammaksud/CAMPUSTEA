"""User model (architecture placeholder).

Planned fields:
- id (PK)
- email (unique)
- student_id (unique)
- password_hash (never exposed)
- anonymous_handle (unique, e.g. "Anonymous Owl")
- role (student | admin)
- status (active | suspended | banned)
- bio, avatar (optional private profile fields)
- created_at, updated_at

Planned relationships:
- posts, comments, likes, reports, notifications
"""

# Model implementation will be added in a future phase.
