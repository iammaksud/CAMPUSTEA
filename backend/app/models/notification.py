"""Notification model (architecture placeholder).

Planned fields:
- id (PK)
- user_id (FK → User, cascade on delete)
- type (like | comment | report | moderation)
- message
- reference_id (optional — post/comment/report id)
- is_read (default false)
- created_at

Planned relationships:
- recipient (User)
"""

# Model implementation will be added in a future phase.
