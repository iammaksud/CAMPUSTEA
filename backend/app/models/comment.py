"""Comment model (architecture placeholder).

Planned fields:
- id (PK)
- post_id (FK → Post, cascade on delete)
- user_id (FK → User)
- content
- created_at, updated_at

Planned relationships:
- post (Post), author (User)
"""

# Model implementation will be added in a future phase.
