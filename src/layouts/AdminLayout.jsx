/**
 * AdminLayout — admin dashboard shell with sidebar.
 * Not wired into App.jsx yet; existing admin layout is preserved in AdminDashboard.jsx.
 */
export default function AdminLayout({ children }) {
  return <>{children}</>
}
